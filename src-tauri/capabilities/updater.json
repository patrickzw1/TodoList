{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Update permissions limited to the TodoList main window",
  "windows": ["main"],
  "permissions": [
    "process:allow-restart",
    "updater:default"
  ]
}
