# TodoList v0.2.17

- 修复 Windows 打包应用环境下 AppData 写入重定向导致 GUI 与 MCP 使用不同数据库的问题。
- 已迁移的共享库优先使用用户主目录下 .todolist 的对应通道目录，数据库与 managed-files 保持同一数据根。
- 正常旧安装在确认路径未被重定向时继续使用原库；检测到重定向时明确阻止使用分叉库，不自动覆盖、合并或迁移数据。
- 新安装由桌面程序首次初始化共享库，MCP 不再提前创建空库；正式与开发数据保持隔离。

升级后请重新连接 MCP 或重启 Codex，使其启动新版 sidecar。已有共享库将自动被识别；仍有旧库分叉的电脑需先备份并完成明确的数据迁移。日志仍保存在可执行文件旁的 logs 目录。
