# TodoList v0.2.18

- 修复 Windows 目录别名、8.3 短路径导致旧数据库路径被误判为重定向的问题。
- 保留新写探针对真实重定向的检测，避免 GUI 与 MCP 再次写入不同的数据目录。
- 包含 v0.2.17 的共享存储修复：优先使用用户主目录下 .todolist 的对应通道数据，数据库与附件使用同一数据根；开发与正式数据保持隔离。
- 新安装由桌面程序初始化数据；升级不会自动覆盖、合并或迁移旧数据库。

安装后请重新连接 MCP 或重启 Codex。已迁移到共享目录的数据会被新版识别；仍存在旧库分叉的电脑需先备份并明确迁移。日志继续保存在程序旁的 logs 目录。
