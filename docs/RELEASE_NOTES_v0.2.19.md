# TodoList v0.2.19

- 修复附件名称与扩展名之间出现过大空隙的问题；短名称自然紧邻，长名称省略时保留扩展名。
- 附件右侧操作改为“在文件夹中显示”，在 Windows 资源管理器中定位并选中 TodoList 保存的附件副本；点击附件名称仍使用默认应用打开文件。
- 改善附件定位失败提示，避免将资源管理器启动失败误报为文件不可用。

已验证中文、空格及 Windows 扩展路径下的文件定位，以及窄窗口内附件名称和操作按钮的显示。
